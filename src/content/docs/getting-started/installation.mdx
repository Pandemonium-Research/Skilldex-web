---
title: "Installation"
description: "How to install the skilldex-cli on macOS, Linux, and Windows."
section: "Getting Started"
order: 2
---

# Installation

The npm package is `skilldex-cli`; it installs the `skillpm` command and its short alias `spm`. Every method needs Node.js 20 or later.

## npm (recommended)

Works on macOS, Linux, and Windows.

```bash
npm install -g skilldex-cli
```

## Homebrew (macOS/Linux)

```bash
brew tap pandemonium-research/skilldex
brew install skilldex-cli
```

## curl (macOS/Linux)

The script checks your environment and installs via npm.

```bash
curl -fsSL https://skilldex-web.vercel.app/install.sh | sh
```

## Scoop (Windows)

Requires [Scoop](https://scoop.sh).

```bash
scoop bucket add skilldex https://github.com/Pandemonium-Research/scoop-skilldex
scoop install skilldex-cli
```

## Verify the install

```bash
skillpm --version
```

This prints the installed version.

## Troubleshooting

**Command not found after npm install**

Make sure your global npm bin directory is in your PATH:

```bash
npm config get prefix
# Add the output path + /bin to your PATH
```

**Permission denied on macOS/Linux**

Avoid using `sudo` with npm. Instead, configure npm to use a directory you own:

```bash
npm config set prefix ~/.npm-global
export PATH=$HOME/.npm-global/bin:$PATH
```

## Next steps

[Your first skill →](/docs/getting-started/first-skill)
